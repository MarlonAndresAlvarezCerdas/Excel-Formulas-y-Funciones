# Excel-Formulas-y-Funciones
You could check the verification project for Cisco Academy on Formulas and functions and Excel
Some formulas use for this proyect was CONCAT (CONCAT), LARGO (LEN), IZQUIERDA (LEFT), DERECHA (RIGHT) y EXTRAE (MID), CONTAR (COUNT), CONTAR.BLANCO (COUNTBLANK), CONTARA (COUNTA), CONTAR.SI (COUNTIF), PROMEDIO.SI (AVERAGEIF), MIN.SI.CONJUNTO (MINIFS) y MAX.SI.CONJUNTO (MAXIFS)
I modify the lab with a list for the functions so is more interactive and complete because you are allow to see where you want
